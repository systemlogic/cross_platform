# Register toolchains
build --extra_toolchains=//:gcc_x86_64_toolchain
build --extra_toolchains=//:gcc_arm64_toolchain

# Define platform shortcuts
build:arm64 --platforms=//:arm64_platform
build:x86_64 --platforms=//:x86_64_platform
